// Define the user model
class User {
    constructor(name, email, coins) {
      this.name = name;
      this.email = email;
      this.coins = coins;
    }
  }
  
  // Export the user model
  export default User;
  